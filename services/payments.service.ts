/**
 * Payments Service
 *
 * Handles payment product listing, source account selection,
 * internal payments, and Payzen/PSE payments.
 *
 * @see /docs/CONSOLIDATED_API_REFERENCE.md#5-payments
 * @see /services/_template.ts for implementation patterns
 */

// import { apiPost } from '@/lib/api/client';

// TODO: Implement endpoints from consolidated reference
// Endpoints to implement:
// - POST /payment/products                    ✅ Used in mobile (payment selection)
// - POST /payment/internal/sources/savings    ✅ Used in mobile (payment source)
// - POST /payment/internal/sources/credits    ✅ Used in mobile (payment source)
// - POST /payment/internal/createTransaction  ✅ Used in mobile (execute payment)
// - POST /payment/payzen/createTransaction    ✅ Used in mobile (PSE payment link)

export {};
