/**
 * Products & Account Service
 *
 * Handles product queries, balances, and transaction movements.
 *
 * @see /docs/CONSOLIDATED_API_REFERENCE.md#4-account--products
 * @see /services/_template.ts for implementation patterns
 */

import { apiPost } from '@/lib/api/client';

// TODO: Implement endpoints from consolidated reference
// Endpoints to implement:
// - POST /balances             ✅ Used in mobile (dashboard)
// - POST /movements            ✅ Used in mobile (product details)
// - POST /products/savings     ✅ Used in mobile (ahorros page)
// - POST /products/credits     ✅ Used in mobile (obligaciones page)
// - POST /products/investments ✅ Used in mobile (inversiones page)
// - POST /products/contributions ✅ Used in mobile (aportes page)
// - POST /products/protection  ✅ Used in mobile (proteccion page)

export {};
